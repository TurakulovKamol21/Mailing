# Mailing Frontend

## Run

```bash
cp .env.example .env
npm install
npm run dev
```

Default URL: `http://localhost:5173`

## Environment

- `VITE_API_BASE_URL` - backend API base url
- `VITE_DEV_PROXY_TARGET` - Vite dev proxy target
